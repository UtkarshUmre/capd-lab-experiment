# capd-lab-experiment
Hands-on exploration of Cluster API (CAPI) using the Docker provider (CAPD). Successfully provisioned a local management cluster and workload clusters with custom feature gates.
